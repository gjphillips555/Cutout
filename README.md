# Cutout

Drop an image. Cut the backdrop. Keep white lettering and enclosed fills.

Studio cut floods from the edges, then fills holes so logos like **LOOTERS** stay solid white. Neural cut (optional) loads a subject model in the browser — nothing is uploaded.

Open `index.html` or the Vercel URL.
